# chats/

Verbatim log of each request made in the chat that produced this project, in order.

Files are named `NN-<brief-summary>-<DATE>.md`. The leading two-digit `NN` keeps them
in chronological order with a normal sort; the date on the end is the timestamp.
(Exact per-message clock times weren't available, so the requests are sequenced by the
order they were sent on 2026-06-29.)

| # | Request |
|---|---|
| 01 | initial site overhaul deresident sort pagination presets fix filtering |
| 02 | prettify format the file |
| 03 | share as loadable artifact link |
| 04 | investigation filters and search still not working |
| 05 | investigation ten item show more missing |
| 06 | investigation do claude artifacts have urls |
| 07 | finder first section nav flare 12mo cost column up down filters |
| 08 | add minimum contract length column and filter |
| 09 | 12mo cost everywhere all tier combos aussie pro rename clickable providers savings sort |
| 10 | monthly cost filter sort split avg ongoing promo |
| 11 | floating back to top button |
| 12 | populate router cost column split router filters into two popups |
| 13 | extract data to json add rescrape md fix leaptel savings |
| 14 | save scrape scripts to scrape directory |
| 15 | save all chat requests to chats folder and push |
